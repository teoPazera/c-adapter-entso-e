# S4–S6 reproducibility release asset

The associated GitHub Release should contain the ZIP archive generated from
the S4–S6 bulk outputs. It includes the LLM response cache, raw/hourly
forecast comparisons, worked example, a manifest, and SHA-256 checksums.

The repository keeps only compact summary results and the visualisation.
No `.env` file or API key is included.
